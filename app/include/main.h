#ifndef MAIN_H
#define MAIN_H
#include <stdbool.h>

// the main program initialize the modules and clean up the modules after the program is terminated

bool isRun(void);
// terminate the program
void Program_terminate(void);

#endif